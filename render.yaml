services:
  - type: web
    name: ibflip
    env: python
    plan: free
    autoDeploy: true
    buildCommand: pip install uv && uv sync --frozen
    startCommand: uv run uvicorn ibflip_web.main:app --host 0.0.0.0 --port $PORT
